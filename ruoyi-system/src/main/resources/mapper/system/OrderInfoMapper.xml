<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ruoyi.system.mapper.OrderInfoMapper">
    <resultMap id="BaseResultMap" type="com.ruoyi.system.domain.OrderInfo">
        <id property="orderId" column="order_id" />
        <result property="userId" column="user_id" />
        <result property="scheduleId" column="schedule_id" />
        <result property="cinemaName" column="cinema_name" />
        <result property="movieName" column="movie_cn_name" />
        <result property="hallName" column="hall_name" />
        <result property="scheduleStartTime" column="schedule_startTime" />
        <result property="orderPosition" column="order_position" />
        <result property="orderTime" column="order_time" />
        <result property="orderState" column="order_state" />
        <result property="orderPrice" column="order_price" />
        <result property="userName" column="user_name" />
        <result property="userMobile" column="user_mobile" />
    </resultMap>
    <select id="selectList" resultMap="BaseResultMap">
         select  o.*,c.cinema_name,m.movie_cn_name,h.hall_name,s.schedule_startTime
          from orderinfo o
         left join schedule s on o.schedule_id = s.schedule_id
         left join movie m on s.movie_id = m.movie_id
         left join hall h on s.hall_id = h.hall_id
          left join cinema c on h.cinema_id = c.cinema_id
<where>
    <if test="userName != null and userName!=''">
        and o.user_name like concat('%',#{userName},'%')
    </if>
    <if test="userMobile != null and userMobile!=''">
    and o.user_mobile like concat('%',#{userMobile},'%')
    </if>
    <if test="cinemaName != null and cinemaName!=''">
        and c.cinema_name like '%${cinemaName}%'
    </if>
    <if test="movieName != null and movieName!=''">
        and m.movie_cn_name like '%${movieName}%'
    </if>


</where>
    </select>
    <select id="findSoldSeat" resultMap="BaseResultMap">
        select * from orderinfo where schedule_id=#{scheduleId} and order_state=1
    </select>
    <insert id="createOrder">
        insert into orderinfo ( order_id, user_id,schedule_id,order_position,order_time,order_state,order_price, user_name, user_mobile)
        values ( #{orderId},#{userId},#{scheduleId},#{orderPosition},#{orderTime},#{orderState},#{orderPrice} ,#{userName},#{userMobile})
    </insert>

</mapper>